## 软件部署

### 环境搭建

+ 环境配置：需要安装docker部署相关的api服务

+ sudo pacman -S docker
  + 注：在安装docker之前需要同步pacman仓库数据库: sudo pacman -Sy 由此保证能正常安装docker

+ 现在docker在国内已经禁用，需要在 /etc 下新建一个docker文件夹并在其中创建文件deamon.json，文件中添加国内镜像：

  + ```
    {
    "registry-mirrors":["https://6kx4zyno.mirror.aliyuncs.com"]
    }	
    ```

  + 这里使用的是阿里云的镜像

+ 部署相关api:

  + ``` 
    1. docker run -d -p 3000:3000 --name netease_cloud_music_api 
    
    2. docker pull binaryify/netease_cloud_music_api
    ```

+ 使用docker:

  + ```
    // 在使用之前需要重启
    systemctl restart docker
    
    // 然后启动 music
    docker start music
    ```

+ 下一步是启动软件



### 软件安装

+ 解压 MyFreems.tar.gz 压缩包
+ 解压完成后的到安装程序 Myfreems 点击运行，然后跟着步骤安装即可



### 启动

+ 在环境搭建好的前提下启动程序即可

 
